#!/bin/sh
# Shareable pre-commit hook. Install with:
#   ln -sf ../../scripts/pre-commit .git/hooks/pre-commit
# (or copy it). Guards game.js ↔ sim.ts physics-constant drift.

node scripts/check-physics-sync.js || exit 1
