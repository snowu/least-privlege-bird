# Supabase CLI local state (per-machine, regenerated by `supabase link`)
supabase/.temp/
supabase/.branches/

# Local stuff
bird.txt
ideas.txt

# Build output (produced by esbuild; CI rebuilds for Pages)
dist/
node_modules/
